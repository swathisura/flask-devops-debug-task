# Problem

The application inside the container fails to start correctly.

Your goal is to debug and fix the issues so that:
- The application runs successfully
- The service is accessible via HTTP

You may inspect logs, dependencies, and configuration files.

All files are located in /app directory.
